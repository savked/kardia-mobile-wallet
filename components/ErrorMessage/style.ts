import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    text:{
        color: 'red', 
        fontSize: 14, 
        fontStyle: 'italic',
        textAlign:'left',
        minWidth:'100%',
        marginTop:5
    }
})

