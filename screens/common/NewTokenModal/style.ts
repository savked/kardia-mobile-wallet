import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // padding: 12,
    flex: 1,
    justifyContent: 'flex-start',
    width: '100%',
  },
  textIcon: {
    position: 'absolute',
  },
  headline: {
    // fontWeight: '500',
    marginBottom: 5,
  },
});
