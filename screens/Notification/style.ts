import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    listCard: {
        display: 'flex',
        flexDirection:'row',
        justifyContent:'space-around',
    },
    thumbnail: {
        width:50,
        height:50
    }
})
