import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    dateText: {
        color: '#b0acac',
        fontSize: 11,
        fontWeight: 'bold'
    },
    dateContainer: {
        alignItems: 'center',
        justifyContent: 'center'
    },
    kaiAmount: {
        fontWeight: 'bold'
    }
})